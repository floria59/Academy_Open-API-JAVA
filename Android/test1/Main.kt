package com.example.androidlab.test1

import java.util.*

fun main() {
    print("Hello world!")

    // 패키지가 동일하면, 변수, 함수도 사용이 가능하다.
    data=10
    formatDate(Date()) // 기본적으로 제공하는 라이브러리를 사용시 import 단축키는
                        // ctrl + Enter 키이다.
}